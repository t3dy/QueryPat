# Dependencies
node_modules/
site/node_modules/

# Build output
site/dist/

# Database (rebuilt from pipeline)
database/unified.sqlite
database/unified.sqlite-wal
database/unified.sqlite-shm
database/unified.sqlite.bak*

# Generated markdown corpus (regenerable; ~37 MB)
database/extracted_markdown/

# Audit reports (rebuilt)
audit_report.json
audit_report.md

# OS files
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/

# Python
__pycache__/
*.pyc
*.pyo

# Local environment (Supabase keys)
.env
.env.local
.env.*.local
site/.env
site/.env.local
site/.env.*.local

# Claude internal: local settings and worktrees stay out, but the layered
# agent-context system (rules/agents/workflows/context) is part of the repo —
# a fresh checkout must be able to operate on this project without help.
.claude/*
!.claude/rules/
!.claude/agents/
!.claude/workflows/
!.claude/context/

# PDFs and source materials (too large for git, kept locally)
PKD LLM Chats/
PKD stuff to add/

